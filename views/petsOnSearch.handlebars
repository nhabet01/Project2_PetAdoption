
 <!-- Navigation -->

     <div class="shadow">
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">

     
       <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                    <img src="../static/images/petfinderLogoSmall.png" alt="">
                </a>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li>
                        <a href="/logout">Log Out</a>
                    </li>
                    <li>
                        <a href="/">Contact</a>
                    </li>
                    <li>
                    <!-- need to pass in username to add to href=/search/username -->
                        <a href="/search/{{user.username}}">Search</a>
                    </li>
                    
                     <li>
                    <!-- need to pass in username to add to href=/search/username -->
                        <a href="/favorites/{{user.username}}">Favs</a>
                    </li>
                   
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>

    <div class="container-fluid text-center">
        <div class="jumbotron-fluid">
            <h1>Pets</h1>
            <br>
           <p>{{user.username}}'s favorites userid:{{user.userid}} </p> <!-- If user ID not needed to pass info along then will not include here -->
                                    {{#if Nofavs}}
                                        <h1>{{Nofavs}}</h1>
                    
                    
                                       {{/if}}
           {{#if favs }}
                        <h3>   Your Favorites </h3>
                {{#each favs}}
                             <div class=" favorites col-lg-3 col-md-4 col-xs-8 thumb crop">
                <img class="petPicture thumbnail" width="300px" height="300px" src="{{this.petPicture}}" alt="">
                <p class="phone">Phone: {{this.phone}}</p>
                <p>Email: <a href="mailto:{{this.email}}" target ="_top">{{this.email}}</a> </p> 
                <p>Address: {{this.address}}</p>
                <form method="POST" action="/favorites/{{../user.userid}}/{{../user.username}}/{{this.petid}}?_method=DELETE">
  <button type="submit">Remove</button>
</form>

            </div>
                {{/each}}
           {{/if}}




           {{#if pets}} 
                    <h3> We Found Animals in your area!  </h3> 
                    {{/if}}
            {{#each pets}}

            <div class="col-lg-3 col-md-4 col-xs-8 thumb crop">
                <img class="petPicture thumbnail" width="300px" height="300px" src="{{this.petPicture}}" alt="">
                <p class="phone">Phone: {{this.phone}}</p>
                <p>Email: <a href="mailto:{{this.email}}" target ="_top">{{this.email}}</a> </p> 
                <p>Address: {{this.address}}</p>

                <div class="favs">
                 <!-- need to 1.) find out what the pet id is called and place somewhere in that form (maybe where it is maybe somewhere else) 2.) create a post route for foundAnimals/...so that an entry is recorded in the favorites table. 3.) need to add a boolean to the table? maybe not...but must associate the pet with user and many users may like the same pet so have to change the associations-->
                    <form action="/favAnimals/" method="POST">
                      <input type="hidden" name="favorite" value="{{this.petid}}${{this.petuserid}}${{this.petusername}}">
                      <button type="submit">Favorite!<span class="glyphicon glyphicon-thumbs-up"></span></button>

                    </form>
                    <br>
                </div>

            </div>
            {{/each}}
        </div>
    </div>

   <!--create fxn in api call that accepts animal id and returns data,-->